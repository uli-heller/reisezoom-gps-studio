pywebview>=6.0
gpxpy>=1.6
piexif>=1.1.3
Pillow>=10.0
# v0.9.57 — HEIC/HEIF-Decoder als Pillow-Plugin. Wird im Bundle mitgeliefert,
# damit User OHNE installiertes exiftool HEIC-Fotos (iPhone) öffnen können.
pillow-heif>=0.16
playwright>=1.50
requests>=2.32
# Liefert ein statisch gelinktes ffmpeg-Binary als Fallback wenn der User
# kein System-ffmpeg installiert hat. PyInstaller bundelt das Binary
# automatisch (collect_data_files in der .spec).
imageio-ffmpeg>=0.5

# macOS-only: PyObjC für NSOpenPanel + NSWindow-Manipulation
pyobjc>=11.0; sys_platform == "darwin"

# Windows-only: pywebview-Backend EdgeChromium
pywebview[edge]>=6.0; sys_platform == "win32"

# Linux: pywebview braucht GTK oder QT. GTK ist gängiger auf Ubuntu/Debian.
# (Falls dein Distri pyobject nicht hat: `apt install python3-gi gir1.2-webkit2-4.1`)
pywebview[qt]>=6.0; sys_platform == "linux"
